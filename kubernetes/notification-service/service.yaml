apiVersion: v1
kind: Service
metadata:
  name: notification-service-service
  namespace: application-namespace

spec:
  selector:
    app: notification-service

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8040