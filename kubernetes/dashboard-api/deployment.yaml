apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-api-deployment
  labels:
    app: dashboard-api
    environment: production
  namespace: application-namespace
  annotations:
    checkov.io/skip1: CKV_K8S_43=I will add commits when I apply the aws infrastructure
    checkov.io/skip2: CKV_K8S_14=I will add commits when I apply the aws infrastructure
    checkov.io/skip3: CKV_K8S_35

spec:
  replicas: 2
  selector: 
    matchLabels:
      app: dashboard-api 


  template:
    metadata:
      labels:
        app: dashboard-api

    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10000
        runAsGroup: 30000
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: dashboard-api-service-account
      containers:
      - name: dashboard-api
        image: 499024813019.dkr.ecr.eu-west-2.amazonaws.com/dashboard-api:d3871de576a145717a665667415a7488143a2ec0-33
        imagePullPolicy: Always
        securityContext:
            runAsNonRoot: true
            runAsUser: 10000
            runAsGroup: 30000
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop: ["ALL"]
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
        ports:
        - containerPort: 8086
        envFrom:
          - secretRef:
              name: application-secret

        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"

        livenessProbe:
          httpGet:
            path: /healthz
            port: 8086
          initialDelaySeconds: 130
          periodSeconds: 5

        readinessProbe:
          tcpSocket:
            port: 8086
          initialDelaySeconds: 20
          periodSeconds: 15