apiVersion: v1
kind: Service
metadata:
  name: payment-service-service
  namespace: application-namespace

spec:
  selector:
    app: payment-service

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8083