apiVersion: v1
kind: Service
metadata:
  name: dashboard-api-service
  namespace: application-namespace

spec:
  selector:
    app: dashboard-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8086