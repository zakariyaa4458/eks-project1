apiVersion: v1
kind: Service
metadata:
  name: inventory-service-service
  namespace: application-namespace

spec:
  selector:
    app: inventory-service

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082