apiVersion: v1
kind: Service
metadata:
  name: scheduler-service
  namespace: application-namespace

spec:
  selector:
    app: scheduler

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8091