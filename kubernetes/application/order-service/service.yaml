apiVersion: v1
kind: Service
metadata:
  name: order-service-service
  namespace: application-namespace

spec:
  selector:
    app: order-service

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081