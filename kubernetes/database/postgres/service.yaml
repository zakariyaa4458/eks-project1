apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: database-ns

spec:
  clusterIP: None

  ports:
      - port: 5432
        name: postgres

  selector:
        app: postgres