persistence:
  enabled: true
  size: 10Gi
  storageClassName: standard

datasources:
  datasources.yaml:
    apiVersion: 1
    dataSources:
      - name: Prometheus
        type: prometheus
        url: http://prometheus-server.monitoring-ns.svc.cluster.local
        isDefault: true